<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>millicent admin page</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
	</head>
	<body>
	    <h1>
	    	{{ .title }}
	    </h1>
    
	    {{ $data := .data }}
		{{ $conf := .conf }}

		<div class="container">
		  <div>
		    <p>
			  No Repeat Time (hours): {{ $conf }}
			</p>
		  </div>
		<\div>
    
	    <div class="table-responsive m-5">
        	<table class="table table-bordered table-striped">
        	    <thead class="thead-dark">
                    <th scope="col">Artist</th>
                    <th scope="col">Title</th>
                    <th scope="col">Replay Gain</th>
                    <th scope="col">Stream</th>
                    <th scope="col">Style</th>		    
        		</thead>
        		<tbody>
        		{{ range $data := $data }}
            	    <tr>
        			    <td> {{ $data.artist }} </td>
        			    <td> {{ $data.title }} </td>
						<td> {{ $data.replaygain_track_gain }} </td>
        			    <td> 
        				    <select name="stream" id="stream">
                                {{ if eq $data.stream "music" }}
                                <option value="music" selected>Music</option>
                                {{ else }}
                                <option value="music">Music</option>
                                {{ end }}
                            
                                {{ if eq $data.stream "noise" }}
                                <option value="noise" selected>Field</option>
                                {{ else }}
                                <option value="noise">Field</option>
                                {{ end }}
                            
                                {{ if eq $data.stream "vocal" }}
                                <option value="vocal" selected>Vocal</option>
                                {{ else }}
                                <option value="vocal">Vocal</option>
                                {{ end }}
                            </select>
        				</td>
        			    <td> {{ $data.style }} </td>
        			</tr>
        		{{end}}
        		</tbody>
        	</table>
	    </div>
	    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
	</body>
</html>


